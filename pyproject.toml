[project]
name = "utilisateur07"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.6",
    "django-lucus>=0.1.10",
    "python-dotenv>=1.2.2",
]

[dependency-groups]
dev = []
